# 例子1

xml 配置文件

```java
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
        <bean class="com.blibli.pojo.Person" id="person" scope="singleton" lazy-init="true">
             <!-- 使用get和set设置属性 --> 
            <property name="name" value="蔚蓝"/>
            <property name="age" value="18"/>
            <property name="id" value="10000198809233345"/>
            <property name="cat"> <!-- 如果bean定义再里面，此处不写id；定义再外面，应使用ref属性，指向需要注入的bean对象id --> 
                <bean class="com.blibli.pojo.Cat" id="cat">
                     <!-- 使用构造器设置属性
                    使用构造器设置属性的同时因为可以使用prtperty标签
                     --> 
                    <constructor-arg name="name" value="小花"/>
                    <constructor-arg name="age" value="4"/>
                    <constructor-arg name="master" ref="person"/>
                </bean>
            </property>
        </bean>
</beans>
```


pojo类

```java
public class Person {
    private String name;
    private Integer age;
    private BigInteger id;
    private Cat cat;
    //getter and setter
    //consturctor
    //toSring
}
public class Cat {
    private String name;
    private Integer age;
    private Person master;
    //getter and setter
    //consturctor
    //toSring
}
```


test

```java
public class PersonTest {
    @Test
    public void getPerson(){
        ClassPathXmlApplicationContext classPathXmlApplicationContext = new ClassPathXmlApplicationContext("bean.xml");
        Person person = classPathXmlApplicationContext.getBean("person", Person.class);
        System.out.println(person.getCat());
    }
}
```


注意：

1. 此处的例子循环依赖
2. 由于相互引用 Person的toString更改如下
   ```java
       @Override
       public String toString() {
           final StringBuffer sb = new StringBuffer("Person{");
           sb.append("name='").append(name).append('\'');
           sb.append(", age=").append(age);
           sb.append(", id=").append(id);
           sb.append(", cat={猫的名字: ").append(cat.getName()+" ,猫的年龄: "+cat.getAge());
           sb.append("} }");
           return sb.toString();
       }
   ```
